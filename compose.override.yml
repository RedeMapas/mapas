# Development overrides
# Frontend and Caddy services are defined in compose.yaml
# Use this file for environment-specific overrides

services:
  mapas:
    environment:
      BASE_URL: "https://mapas.localhost:8443/"
