name: cypress-ci

on:
  pull_request:
    branch:
      - 'main'

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Instala as dependências e executa testes
        run: cd src/ && npm install && npx cypress run --spec "cypress/e2e/**/*.js"