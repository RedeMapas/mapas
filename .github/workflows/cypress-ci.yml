name: cypress-ci

on:
  push:
    branch:
      - '*'
  pull_request:
    branch:
      - '*'

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Instala as dependências, cria configs e executa testes
        run: cd src/ && npm install && ls -al && npx cypress run --spec "/src/cypress/e2e/opportunityPage/index.cy.js"